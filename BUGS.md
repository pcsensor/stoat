# 缺陷记录

## RADIO-2026-09-03-01：分享邀请码后语音频道重连

- 状态：已修复
- 影响：语音频道内打开系统分享页并回到应用时，通话会短暂断开并重新加入。
- 复现：加入任一语音频道，点击“邀请朋友”邀请码卡片，关闭或完成系统分享后返回 Radio。
- 根因：应用从 `inactive/background` 回到 `active` 时会调用语音恢复逻辑；该逻辑没有区分健康的 LiveKit `Room`，会先销毁它再重新连接。
- 修复：仅在没有现有房间、或 SDK 已放弃重连时重建连接；健康房间和 SDK 自愈中的房间保持不变。
- 回归覆盖：`tests/voice-connection-state.test.mjs`。

## 同次审查修复

- 重新进入同一语音频道会重复创建服务器邀请码。
- 频道切换期间完成的搜索、历史分页或发送失败可能把旧频道状态写入新频道。
- 一次新的登录尝试失败会删除此前已安全保存的会话，即使该会话本身仍有效。
